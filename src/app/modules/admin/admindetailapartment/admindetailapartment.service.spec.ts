/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AdmindetailapartmentService } from './admindetailapartment.service';

describe('Service: Admindetailapartment', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AdmindetailapartmentService]
    });
  });

  it('should ...', inject([AdmindetailapartmentService], (service: AdmindetailapartmentService) => {
    expect(service).toBeTruthy();
  }));
});
