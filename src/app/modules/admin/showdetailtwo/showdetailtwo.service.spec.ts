/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ShowdetailtwoService } from './showdetailtwo.service';

describe('Service: Showdetailtwo', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ShowdetailtwoService]
    });
  });

  it('should ...', inject([ShowdetailtwoService], (service: ShowdetailtwoService) => {
    expect(service).toBeTruthy();
  }));
});
