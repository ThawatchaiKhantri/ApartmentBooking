/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EditProfileapartmentService } from './edit-profileapartment.service';

describe('Service: EditProfileapartment', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EditProfileapartmentService]
    });
  });

  it('should ...', inject([EditProfileapartmentService], (service: EditProfileapartmentService) => {
    expect(service).toBeTruthy();
  }));
});
