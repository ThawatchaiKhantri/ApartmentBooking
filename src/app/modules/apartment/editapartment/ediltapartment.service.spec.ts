/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EdiltapartmentService } from './ediltapartment.service';

describe('Service: Ediltapartment', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EdiltapartmentService]
    });
  });

  it('should ...', inject([EdiltapartmentService], (service: EdiltapartmentService) => {
    expect(service).toBeTruthy();
  }));
});
